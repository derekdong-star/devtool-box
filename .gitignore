# ── 编译产物 ──────────────────────────────────────────────────
devtoolbox
devtoolbox.exe
server

# ── 本地持久化数据（含敏感连接信息） ───────────────────────────
# 存储 DB / Redis 连接配置（含 DSN、密码），禁止提交
db_conns.json
# 存储命令模板，属于个人本地数据，禁止提交
cmd_templates.json
# 存储图片 API 配置（含 API Key），禁止提交
image_config.json
# Docker 运行时数据目录
data/

# ── Go 工具链 ─────────────────────────────────────────────────
vendor/

# ── 系统 / IDE ────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp
*.swo
.env

AGENTS.md
