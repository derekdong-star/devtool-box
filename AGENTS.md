# AGENTS.md

## 上下文加载规则

每次 agent 会话开始处理本仓库任务前，先读取：

1. `AGENTS.md`
2. `docs/PROJECT_CONTEXT.md`

按任务类型追加读取：

- 涉及部署、发布、CI/CD、远端服务器、Docker 镜像：读取 `docs/DEPLOY_NOTES.md` 和 `.github/workflows/deploy.yml`
- 涉及 UI、品牌、视觉、布局：读取 `DESIGN.md`
- 涉及用户文档、功能总览、本地数据说明：读取 `README.md`

## 提交规则

### 触发词：「提交」

当我说**提交**时，**不要立即执行 git 操作**，而是先对当前待提交内容进行 Review，流程如下：

1. 执行 `git diff` 和 `git status`，获取完整变更内容
2. **加载并应用 clean-code、refactoring 等 skill 规范**，以其原则（命名、函数大小、DRY、Guard Clauses、魔法数字、anti-patterns 等）作为审查基准
3. 从以下三个维度进行审查：

   **架构整洁**
   - 分层是否清晰（handler / service / model 职责有无越界）
   - 是否有重复代码、冗余逻辑
   - 错误处理是否统一、是否有被静默丢弃的 error
   - 命名是否一致（变量、函数、文件）

   **扩展性**
   - 新增功能是否依赖具体类型而非接口
   - 模块注册方式是否符合现有约定（在 `app.go` 的 `modules` 追加）
   - 硬编码的魔法数字、字符串是否应提取为常量

   **功能**
   - 前后端接口是否对齐（请求/响应格式、字段名）
   - 边界条件是否处理（空值、非法输入、并发）
   - 前端 JS 语法是否正确（可用 `node --check` 验证）
   - 是否影响已有功能

3. 给出 Review 结论：
   - 若发现问题：列出问题清单，提出优化方案，**等待确认**
   - 若无问题：说明"未发现问题，可以提交"，**等待确认**

---

### 触发词：「确认提交」

当我说**确认提交**时，执行以下操作：

1. 用一句话总结本次变更的核心内容（作为 commit message）
2. 执行：
   ```
   git add .
   git commit -m "<总结内容>"
   git push
   ```
3. 推送完成后，输出 commit hash 和变更文件数量

---

## 方案先行规则

### 触发场景

当用户提出涉及**新增模块、重构架构、跨文件改动**等复杂需求时（例如「添加 XX 模块」「重构 XX」），**不要立即写代码**，而是先输出一份具体可落地的实现方案。

### 方案内容必须包含

1. **改动范围**：列出所有新增、修改、删除的文件
2. **接口契约**：前后端请求/响应格式、路由、字段名
3. **数据结构**：新增 DTO、配置项、环境变量
4. **依赖关系**：新模块如何注册到现有架构中
5. **验证计划**：build、JS check、docker compose config 等

### 用户确认后再动手

- 输出方案后，**等待用户确认**或提出调整
- 用户说「可以」「确认」「动手」「开始写」等确认词后，再进入代码实现
- 若用户对方案有调整，更新方案并再次确认
